#include <iostream>
#include <cmath>
#include <cassert>
using namespace std;

void swap(int myArray[], int startIndex, int endIndex);{

    int x = myArray[startIndex];

    myArray[startIndex] = myArray[endIndex];
    myArray[endIndex] = x
    }

int main(){
    assert(reverseArray(myArray[endIndex] == x));
    assert(reverseArray(myArray[startIndex] == myArray[endIndex]))
    
}